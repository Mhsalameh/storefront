import { Typography } from '@mui/material';

export default function Header() {
  return (
    <header>
      <Typography align='center' variant='h2'>
        My Virtual Store's Name
      </Typography>
    </header>
  );
}
